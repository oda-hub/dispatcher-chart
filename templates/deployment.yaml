apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chart.fullname" . }}
  labels:
{{ include "chart.labels" . | indent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "chart.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
      #type: RollingUpdate
    type: Recreate # better RollingUpdate, but needed for once mounted
  template:
    metadata:
      # https://helm.sh/docs/howto/charts_tips_and_tricks/#automatically-roll-deployments
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        app.kubernetes.io/name: {{ include "chart.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ template "chart.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      volumes:
      - name: dispatcher-config-volume 
        configMap:
          name: dispatcher-config
      - name: dispatcher-users
        configMap: 
          name: dispatcher-users
      - name: dispatcher-conf-secret
        secret:
          secretName: dispatcher-conf      
      - name: dispatcher-renku-ssh-key
        secret:
          secretName: dispatcher-renku-ssh-key
      - name: dispatcher-scratch-root
        persistentVolumeClaim:
          claimName: dispatcher-scratch-root
      - name: filelogdir
        persistentVolumeClaim:
          claimName: dispatcher-filelogdir
      - name: homedir
        emptyDir: {}
      {{- if .Values.dda.enable }}
      - name: {{ .Values.dda.ddcache.pvc_name }}
        persistentVolumeClaim:
          claimName: data-reduced-ddcache
      {{- end }}
      
      {{ if .Values.async_dispatcher }}
      - name: redis-data
        persistentVolumeClaim:
          claimName: redis-data
      {{ end }}

      initContainers:
        - name: {{ .Chart.Name }}-scratch-cleaner
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # Setting restartPolicy: Always makes this a sidecar container.
          # https://kubernetes.io/docs/concepts/workloads/pods/sidecar-containers/
          restartPolicy: Always
          command: ["bash"]
          args:
          - "-c"
          - |
            set -x
            while true; do
              date             
              df -h /data/dispatcher_scratch
              find -name query_output\* -ctime +1 -exec rm -fv '{}' \;
              find -maxdepth 1 -name download_\* -ctime +1 -exec rm -rfv '{}' \;
              find -maxdepth 1 -name scratch_sid_\* -ctime +4 -exec rm -rfv '{}' \;
              find -maxdepth 1 -name .lock_\* -ctime +1 -exec rm -fv '{}' \;
              find -wholename \*scratch_sid_statuscaketeststatuscaketest_jid\* -amin +60 -exec rm -rfv '{}' \;
              sleep 1h
            done

          ports: []
          livenessProbe:
            exec:
              command:
              - df 
              - -h
              - /data/dispatcher_scratch
            failureThreshold: 5
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 30

          readinessProbe:
            exec:
              command:
              - df 
              - -h
              - /data/dispatcher_scratch
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 10
          
          volumeMounts:
          - mountPath: /data/dispatcher_scratch
            name: dispatcher-scratch-root

        {{- if .Values.kg_registrar.enabled }}
        - name: kg-registrar
          # Setting restartPolicy: Always makes this a sidecar container.
          restartPolicy: Always
          image: "{{ .Values.kg_registrar.image.repository }}:{{ .Values.kg_registrar.image.tag }}"
          imagePullPolicy: {{ .Values.kg_registrar.image.pullPolicy }}
          ports:
            - name: registrar
              containerPort: {{ .Values.kg_registrar.ports.container }}
          livenessProbe:
            httpGet:
              path: /openapi.json
              port: registrar
            failureThreshold: 5
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 30

          readinessProbe:
            httpGet:
              path: /openapi.json
              port: registrar
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 10

          volumeMounts:
          - mountPath: /data
            name: dispatcher-scratch-root
        {{- end }}
        
        {{- if .Values.usageMonitor.enabled }}
        - name: usage-monitor
          image: "{{ .Values.usageMonitor.image.repository | default "ghcr.io/alexgugnin/oda-usage-monitor" }}:{{ .Values.usageMonitor.image.tag | default "latest" }}"
          restartPolicy: Always
          env:
            - name: SCRATCH_DIR
              value: {{ .Values.usageMonitor.scratchDir | default "/data/dispatcher_scratch" | quote }}
            - name: DB_HOST
              value: {{ .Values.usageMonitor.db.host | default "mysql" | quote }}
            - name: DB_USER
              value: {{ .Values.usageMonitor.db.user | default "stats_bot" | quote }}
            - name: DB_NAME
              value: {{ .Values.usageMonitor.db.name | default "mmoda_usage" | quote }}
            
            {{- if and .Values.usageMonitor.db.existingSecret .Values.usageMonitor.db.existingSecret.name }}
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.usageMonitor.db.existingSecret.name | quote }}
                  key: {{ .Values.usageMonitor.db.existingSecret.key | default "db-password" | quote }}
            {{- else if .Values.usageMonitor.db.password }}
            - name: DB_PASS
              value: {{ .Values.usageMonitor.db.password | quote }}
            {{- end }}
          
          ports: []
          livenessProbe:
            exec:
              command:
              - df 
              - -h
              - /data/dispatcher_scratch
            failureThreshold: 5
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 30

          readinessProbe:
            exec:
              command:
              - df 
              - -h
              - /data/dispatcher_scratch
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 10
            
          volumeMounts:
            - name: dispatcher-scratch-root 
              mountPath: {{ .Values.usageMonitor.scratchDir | default "/data/dispatcher_scratch" | quote }}
              readOnly: true
        {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            failureThreshold: 5
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 10
          resources:
            {{- toYaml .Values.resources.dispatcher | nindent 12 }}
          env:
          - name: HOME
            value: /tmp/oda
          - name: ODA_SPARQL_ROOT
            value: {{ .Values.oda_sparql_root }}
          - name: DISPATCHER_MOCK_KB
            value: "yes"
          - name: DISPATCHER_GUNICORN
            {{- if .Values.dispatcher_gunicorn }}
            value: "yes"
            {{- else }}
            value: "no"
            {{- end }}
          - name: DISPATCHER_ASYNC_ENABLED
            {{- if .Values.async_dispatcher }}
            value: "yes"
            {{- else }}
            value: "no"
            {{- end }}
          {{ if .Values.dda.enable }}
          - name: DDA_USER_TOKEN
            valueFrom:
              secretKeyRef:
                name: dda-interface-token
                key: token.txt
          {{- end }}   
          {{- range $idx, $content := default (list) .Values.extra_env_var }} 
          - name: {{ $content.name }}
            value: "{{ $content.value }}"
          {{- end }}
          volumeMounts:
          - mountPath: /var/log/containers
            name: filelogdir
          - mountPath: /data/dispatcher_scratch
            name: dispatcher-scratch-root
          {{- if .Values.dda.enable }}
          - mountPath: /data/reduced/ddcache
            name: data-reduced-ddcache
          {{- end }}

          - mountPath: /dispatcher/conf/conf_env.yml
            name: dispatcher-conf-secret
            subPath: conf_env.yml
            readOnly: true
          - mountPath: /etc/renku-ssh-key
            name: dispatcher-renku-ssh-key
            subPath: renku-ssh-key
            readOnly: true
          - mountPath: /tmp/oda
            name: homedir
          
          {{- range $plugin := list "osa" "magic" "polar" "antares" "spiacs" "gw" "legacysurvey" "nb" }}
          - name: dispatcher-config-volume
            mountPath: /dispatcher/conf/conf.d/{{$plugin}}_data_server_conf.yml
            subPath: {{$plugin}}_data_server_conf.yml
            readOnly: true
          {{- end }} 


       {{ if .Values.async_dispatcher }}
        - name: redis
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: redis
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
          - name: redis
            containerPort: 6379
            protocol: TCP
          livenessProbe:
            tcpSocket:
              port: redis
            failureThreshold: 5
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 30
          readinessProbe:
            tcpSocket:
              port: redis
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 10
          resources:
            {{- toYaml .Values.resources.redis | nindent 12 }}
          env: []
          volumeMounts:
          - name: redis-data
            mountPath: /data


        - name: celery
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          command: ["celery"]
          args: ["-A", "cdci_data_analysis.flask_app.tasks.celery", "worker", "-E"]
          #args: ["-A", "cdci_data_analysis.flask_app.tasks.celery", "worker", "-E", "--autoscale=30,5"]

          readinessProbe:
            exec:
              command:
              - "/bin/sh"
              - "-c"
              - "celery -A cdci_data_analysis.flask_app.tasks.celery status | grep -o ': OK'"
            initialDelaySeconds: 50
            periodSeconds: 30

          livenessProbe:
            exec:
              command:
              - "/bin/sh"
              - "-c"
              - "celery -A cdci_data_analysis.flask_app.tasks.celery status | grep -o ': OK'"
            initialDelaySeconds: 60
            periodSeconds: 30

          resources:
            {{- toYaml .Values.resources.celery | nindent 12 }}
        

        - name: celery-flower
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          command: ["celery"]
          args: ["-A", "cdci_data_analysis.flask_app.tasks.celery", "flower", "-l", "info", "--port=5555"]

          ports:
            - name: http
              containerPort: 5555
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            failureThreshold: 5
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 10

          resources:
            {{- toYaml .Values.resources.celery_flower | nindent 12 }}


      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
